# Patches

Keep our downstream Chromium changes here as ordered `*.patch` files.

The build applies patches alphabetically after resetting to the exact pinned Chromium revision. Keep extension-enablement changes separate from later privacy/minimalism removals so upstream sync failures stay easy to diagnose.
